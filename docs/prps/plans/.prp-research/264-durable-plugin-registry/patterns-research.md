| Category                | File:Lines                                                                                        | Pattern                                                                                                                                                                                                 | 5-lines-max snippet                                                                                                                                                                                                                          | Applies To                                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Similar Implementations | `packages/praxrr-app/src/lib/server/plugins/registry.ts:35-55`                                    | Namespace by exact `apiVersion`, normalize only the identity lookup key with `id.toLowerCase()`, and reject same-namespace duplicates. Preserve original manifest fields.                               | `const namespace = this.namespaceFor(manifest.apiVersion);`<br>`const key = manifest.id.toLowerCase();`<br>`if (namespace.has(key)) {`<br>`throw new Error(\`Duplicate plugin id ...\`);`<br>`}`                                             | Durable composite key, repository lookup, reconciliation, HTTP entity lookup               |
| Similar Implementations | `packages/praxrr-app/src/lib/server/plugins/scan.ts:81-108`                                       | Keep filesystem scanning validation-free and bounded; return raw entries and let the host own validation/reconciliation. Missing directory is an empty scan; unexpected I/O errors propagate.           | `export async function scanPluginDir(` <br>`dir: string,`<br>`deps: ScanDeps = DEFAULT_SCAN_DEPS`<br>`): Promise<readonly RawManifestEntry[]> {`<br>`const entries: RawManifestEntry[] = [];`                                                | Reload candidate construction and failure-safe snapshot publication                        |
| Similar Implementations | `packages/praxrr-app/src/lib/server/db/migrations/20260711_create_quality_goal_bindings.ts:19-31` | App-DB tables use SQL migrations with snake_case columns, explicit checks/defaults, composite primary keys, and bookkeeping timestamps. This is not a PCD/base-op migration.                            | `CREATE TABLE quality_goal_bindings (`<br>`database_id INTEGER NOT NULL,`<br>`profile_name TEXT NOT NULL,`<br>`arr_type TEXT NOT NULL,`<br>`PRIMARY KEY (database_id, profile_name, arr_type)`                                               | New `plugin_registry` migration keyed by `(api_version, plugin_id COLLATE NOCASE)`         |
| Similar Implementations | `packages/praxrr-app/src/lib/server/db/queries/qualityGoalBindings.ts:7-30`                       | Query modules colocate exported DB row interfaces, camelCase input interfaces, and a named `*Queries` object; row fields retain SQL snake_case.                                                         | `export interface QualityGoalBindingRow {`<br>`database_id: number;`<br>`profile_name: string;`<br>`arr_type: string;`<br>`}`                                                                                                                | Plugin repository row/input types and list/get/update/reconcile methods                    |
| Similar Implementations | `packages/praxrr-app/src/lib/server/db/db.ts:213-226`                                             | Use the app DB manager's transaction wrapper for synchronous multi-statement reconciliation; a throw rolls back and is rethrown. Do not publish the candidate registry before it commits.               | `async transaction<T>(fn: () => T \| Promise<T>): Promise<T> {`<br>`this.beginTransaction();`<br>`const result = await fn();`<br>`this.commit();`<br>`return result;`                                                                        | Atomic scan reconciliation and preservation of the prior in-memory snapshot on failure     |
| Similar Implementations | `packages/praxrr-app/src/lib/server/security/dnsTransport.ts:236-246`                             | Single-flight work is represented by a retained promise and cleared in `finally` only when it is still the current promise. Reload needs one global in-flight promise, not per-key work.                | `let work = this.inFlight.get(key);`<br>`if (work === undefined) {`<br>`work = this.resolveAndCache(...);`<br>`this.inFlight.set(key, work);`<br>`}`                                                                                         | Serialized concurrent `PluginHost.reload()` calls                                          |
| Naming Conventions      | `packages/praxrr-app/src/lib/server/plugins/index.ts:1-17`                                        | Server subsystem internals are camelCase files/classes with a single `index.ts` barrel exporting singleton + class + public types/errors.                                                               | `export { pluginHost, PluginHost } from './host.ts';`<br>`export { pluginRegistry, PluginRegistry, type RegisteredPlugin } from './registry.ts';`<br>`export * from './errors.ts';`                                                          | New repository/service/mapper exports without route-to-internal-file sprawl                |
| Naming Conventions      | `packages/praxrr-app/src/lib/server/utils/config/config.ts:80-86,215-220`                         | Environment names are uppercase snake case; config surface is camelCase. `PLUGINS_ENABLED` is default-off and `config.paths.plugins` lazily reads trimmed `PLUGINS_DIR`.                                | `this.pluginsEnabled = Config.parseBooleanEnv(` <br>`Deno.env.get('PLUGINS_ENABLED')`<br>`);`<br>`get plugins(): string {`<br>`return Deno.env.get('PLUGINS_DIR')?.trim() \|\| ...;`                                                         | Feature-off list/reload/mutation semantics and reload scan directory resolution            |
| Type Definitions        | `packages/praxrr-app/src/lib/shared/plugins/types.ts:17-20,69-82`                                 | The pure shared plugin contract is the sole definition of accepted API versions and validated manifest fields. It is readonly and I/O-free; persisted `id`/`name` values are not normalized or trimmed. | `export const PLUGIN_API_VERSION = '1';`<br>`export const SUPPORTED_PLUGIN_API_VERSIONS = ['1'] as const;`<br>`export interface PluginManifest {`<br>`readonly apiVersion: string;`<br>`readonly id: string;`                                | Persistence JSON validation, public response projection, API-version route validation      |
| Type Definitions        | `packages/praxrr-app/src/lib/server/plugins/registry.ts:18-28`                                    | Internal registered entries include sensitive `sourceDir`; public/MCP response types must be separate whitelist shapes and must not structurally expose this interface.                                 | `export interface RegisteredPlugin {`<br>`readonly manifest: PluginManifest;`<br>`readonly sourceDir: string;`<br>`readonly state: PluginLifecycleState;`<br>`readonly registeredAt: string;`                                                | Redacted plugin response mapper and MCP data minimization                                  |
| Type Definitions        | `packages/praxrr-app/src/lib/server/health/responses.ts:8-20,94-100`                              | Wire mappers may derive exact shapes from generated `components` types; routes enforce parity with `satisfies` rather than maintaining an unchecked parallel contract.                                  | `import type { components } from '$api/v1.d.ts';`<br>`export type ConfigHealthTrendsResponse =`<br>`components['schemas']['ConfigHealthTrendsResponse'];`                                                                                    | Plugin list/detail/reload response aliases and route return checks                         |
| Entry Points            | `packages/praxrr-app/src/hooks.server.ts:42-69`                                                   | DB initialization and migrations precede optional plugin initialization; startup wraps the singleton host in warn-and-continue resilience.                                                              | `await db.initialize();`<br>`await runMigrations();`<br>`if (config.pluginsEnabled) {`<br>`try { await pluginHost.initialize(); }`<br>`catch (error) { ... }`                                                                                | Durable repository availability at boot and NF1 startup resilience                         |
| Entry Points            | `packages/praxrr-app/src/hooks.server.ts:253-270`                                                 | `/api/*` routes inherit the existing hook authentication gate. Do not add route-local auth or mark plugin management paths public.                                                                      | `if (isPublicPath(event.url.pathname)) {`<br>`return resolve(event);`<br>`}`<br>`if (!auth.user && event.url.pathname.startsWith('/api')) {`<br>`return new Response(..., { status: 401 });`                                                 | All `/api/v1/plugins*` handlers                                                            |
| Entry Points            | `docs/api/v1/openapi.yaml:62-72,657-712`                                                          | Root OpenAPI registers each concrete URL and `$ref`s feature path documents; feature schemas are likewise registered under `components.schemas`.                                                        | `/mcp:`<br>`$ref: './paths/mcp.yaml#/mcp'`<br>`/sync-history:`<br>`$ref: './paths/sync-history.yaml#/list'`<br>`/config-health/settings:`                                                                                                    | Add plugin tag, list/detail/enable/disable/reload paths, and plugin schemas contract-first |
| Entry Points            | `deno.json:71,96-97`                                                                              | Contract regeneration has two explicit gates: generate app declarations, then bundle spec + generated declarations into `packages/praxrr-api`.                                                          | `"generate:api-types": "npx openapi-typescript ... -o .../v1.d.ts"`<br>`"bundle:api": "deno run -A scripts/bundle-api.ts"`<br>`"publish:api": "deno task bundle:api ..."`                                                                    | F4 portable contract fidelity and package artifact synchronization                         |
| Contracts               | `packages/praxrr-app/src/lib/shared/plugins/validator.ts:113-145,233-251`                         | Every scanned or persisted manifest crosses the same `unknown -> ManifestValidationResult` validator boundary. Validation checks trimmed emptiness but returns original strings unchanged.              | `export function validatePluginManifest(raw: unknown): ManifestValidationResult {`<br>`if (!isPlainObject(raw)) return { ok: false, errors: [...] };`<br>`...`<br>`return { ok: true, manifest };`<br>`}`                                    | Fresh scans, persisted JSON rehydration, and invalid-row exclusion from dispatch           |
| Contracts               | `packages/praxrr-app/src/lib/server/plugins/host.ts:134-148`                                      | Dispatch resolves a versioned registry view at call time. Extend that registry selector to require both durable `enabled` and current `discovered`; enablement alone never means execution succeeded.   | `const plugins = pluginRegistry.listForPoint(` <br>`PLUGIN_API_VERSION, point`<br>`);`<br>`if (plugins.length === 0) return;`<br>`for (const plugin of plugins) await this.dispatchOne(...);`                                                | F7 enabled-and-discovered dispatch filtering                                               |
| Contracts               | `packages/praxrr-app/src/lib/server/sync/syncHistory/responses.ts:43-55`                          | A named response builder is the reusable boundary: query rows are projected once, then both HTTP and MCP call the same mapper instead of duplicating public shapes.                                     | `export function buildSyncHistoryListResponse(` <br>`rows: SyncHistorySummary[],`<br>`opts: { page: number; pageSize: number; total: number }`<br>`): SyncHistoryListResponse {`<br>`return { items: rows.map(toSyncHistorySummary), ... };` | Shared redacted plugin list/detail mapper for HTTP and `list_plugins`                      |
| Contracts               | `packages/praxrr-app/src/lib/server/mcp/tools.ts:123-152,396-427`                                 | MCP tools live in the static `TOOLS` registry, use `additionalProperties: false`, carry `readOnlyHint: true`, directly call services/mappers, and expose no mutation handler.                           | `export interface McpTool {`<br>`name: string;`<br>`inputSchema: JsonSchema;`<br>`annotations: ToolAnnotations;`<br>`handler: (...) => Promise<unknown>;`                                                                                    | Read-only `list_plugins` with the HTTP response mapper and redacted output                 |
| Entry Points            | `.git:1`                                                                                          | This checkout is the shared feature worktree; all planning and implementation changes must stay here, on `feat/264-durable-plugin-registry`, with no child task worktrees.                              | `gitdir: .../.git/worktrees/`<br>`praxrr-264-durable-plugin-registry`                                                                                                                                                                        | Every issue #264 implementation task and generated contract artifact                       |
