| Category           | File:Lines                                                                                  | Pattern                                                                                                                              | 5-lines-max snippet                                                                                                                                                                                                          | Applies To                                                                                                                            |
| ------------------ | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Configuration      | `packages/praxrr-app/src/lib/server/utils/config/config.ts:80-87`                           | Feature flags are parsed once with non-throwing boolean helpers; plugins default off.                                                | `this.mcpEnabled = Config.parseBooleanEnvWithDefault(..., true);`<br>`this.pluginsEnabled = Config.parseBooleanEnv(Deno.env.get('PLUGINS_ENABLED'));`                                                                        | F5; keep list/reload feature-aware and mutation disabled without adding a second flag.                                                |
| Configuration      | `packages/praxrr-app/src/hooks.server.ts:42-64`                                             | App DB initializes and migrates before optional plugin startup.                                                                      | `await db.initialize();`<br>`await runMigrations();`<br>`...`<br>`await pluginHost.initialize();`                                                                                                                            | F1, NF1; repository access is safe during host initialization.                                                                        |
| Configuration      | `packages/praxrr-app/src/hooks.server.ts:61-75`                                             | Optional subsystem startup is warn-and-continue; disabled state is logged without touching discovery.                                | `if (config.pluginsEnabled) {`<br>`  try { await pluginHost.initialize(); }`<br>`  catch (error) { await logger.warn(...); }`<br>`} else { await logger.info(...); }`                                                        | F5, NF1; persistence/discovery failures must not abort boot.                                                                          |
| Dependencies       | `deno.json:2-34`                                                                            | Reuse workspace aliases and existing SQLite/server dependencies; plugin foundation adds no runtime package.                          | `"$server/": "./packages/praxrr-app/src/lib/server/",`<br>`"$db/": "./packages/praxrr-app/src/lib/server/db/",`<br>`"@std/yaml": "jsr:@std/yaml@^1.0.10"`                                                                    | All implementation; no new WASM/Extism or ORM dependency.                                                                             |
| Dependencies       | `packages/praxrr-app/src/lib/server/db/db.ts:1-4,103-107`                                   | Durable app state uses the existing synchronous SQLite `DatabaseManager`, not PCD Kysely/base ops.                                   | `import { Database } from '@jsr/db__sqlite';`<br>`getDatabase(): Database {`<br>`  if (!this.db) throw new DatabaseNotInitializedError();`<br>`  return this.db;`<br>`}`                                                     | F1-F2, NF2; implement a raw-SQL query repository under `$db/queries`.                                                                 |
| Data Flow          | `packages/praxrr-app/src/lib/server/plugins/scan.ts:75-118`                                 | Scanner is the only filesystem boundary, bounded to 256 directories; missing root degrades to empty and unexpected errors propagate. | `for await (const entry of deps.readDir(dir)) {`<br>`  if (!entry.isDirectory) continue;`<br>`  if (scanned >= MAX_PLUGIN_DIRS) { ... }`<br>`  const parsed = await readManifestEntry(...);`<br>`}`                          | F2, F6, NF1; reload must scan successfully before reconciliation.                                                                     |
| Data Flow          | `packages/praxrr-app/src/lib/server/plugins/host.ts:95-125`                                 | Current host scans, validates, and registers sequentially; summary counts are the reload-result precedent.                           | `const entries = await scanPluginDir(dir);`<br>`for (const entry of entries) {`<br>`  const outcome = await this.registerEntry(entry);`<br>`  ...`<br>`}`                                                                    | F2, F6; refactor into scan/validate candidate -> DB reconcile -> atomic snapshot publish.                                             |
| Data Flow          | `packages/praxrr-app/src/lib/server/plugins/host.ts:151-206`                                | Current reset clears before rescan and `registerEntry` mutates the global registry directly.                                         | `reset(): void { pluginRegistry.clear(); }`<br>`...`<br>`pluginRegistry.register(entry.dir, result.manifest);`                                                                                                               | F6; replace clear-first reload so scan/reconcile failure leaves the prior snapshot usable.                                            |
| Data Flow          | `packages/praxrr-app/src/lib/server/plugins/registry.ts:35-55`                              | In-memory identity is `(apiVersion, lowercased id)` with case-insensitive duplicate rejection.                                       | `private readonly byApiVersion = new Map<...>();`<br>`const key = manifest.id.toLowerCase();`<br>`if (namespace.has(key)) throw new Error(...);`<br>`namespace.set(key, entry);`                                             | F1-F2; durable key and replacement snapshot must preserve namespace isolation.                                                        |
| Data Flow          | `packages/praxrr-app/src/lib/server/plugins/registry.ts:71-89`                              | Reads are namespace-qualified; dispatch selection filters extension-point declarations; clear is global.                             | `get(apiVersion, id) { return this.byApiVersion.get(apiVersion)?.get(id.toLowerCase()); }`<br>`listForPoint(apiVersion, point) {`<br>`  return this.listByApiVersion(apiVersion).filter(...);`<br>`}`                        | F3, F7; add enabled+discovered filtering and an atomic whole-snapshot replacement method.                                             |
| Data Flow          | `packages/praxrr-app/src/lib/shared/plugins/validator.ts:126-152,237-251`                   | Validation rejects empty/unsupported identifiers but returns authored strings without trimming.                                      | `apiVersion: apiVersion as string,`<br>`id: id as string,`<br>`name: name as string,`<br>`version: version as string,`                                                                                                       | F1, NF3; revalidate persisted JSON and preserve exact identifiers/names.                                                              |
| Database migration | `packages/praxrr-app/src/lib/server/db/migrations/_template.ts:35-66`                       | App schema changes are `Migration` objects with unique version, focused `up`, and reverse-order `down`.                              | `export const migration: Migration = {`<br>`  version: 999,`<br>`  name: '...',`<br>`  up: \`...\`,`<br>` down: \`...\``                                                                                                     | F1; add a new high-water migration for `plugin_registry` only (no PCD seed registration).                                             |
| Database migration | `packages/praxrr-app/src/lib/server/db/migrations.ts:70-87,322-410`                         | Every migration is statically imported, appended to `loadMigrations`, then version-sorted.                                           | `import { migration as migration20260723... } from './migrations/...';`<br>`const migrations: Migration[] = [`<br>`  ...`<br>`  migration20260723...,`<br>`];`                                                               | F1; register the new migration in both locations and recheck high-water after rebase.                                                 |
| Database migration | `packages/praxrr-app/src/lib/server/db/migrations/20260717_create_webauthn_tables.ts:37-44` | Case-insensitive scoped uniqueness is expressed with `COLLATE NOCASE` on a composite unique index.                                   | `name TEXT NOT NULL CHECK (...),`<br>`...`<br>`CREATE UNIQUE INDEX ...`<br>`  ON webauthn_credentials(user_id, name COLLATE NOCASE);`                                                                                        | F1; enforce `(api_version, plugin_id COLLATE NOCASE)` in SQLite, not only in TypeScript.                                              |
| Database query     | `packages/praxrr-app/src/lib/server/db/db.ts:133-162`                                       | Query repositories use typed `query`/`queryFirst` and parameterized `execute`; booleans are SQLite integers.                         | `queryFirst<T>(sql, ...params): T                                                                                                                                                                                            | undefined { ... }`<br>`execute(sql, ...params): number {`<br>` const stmt = this.prepare(sql);`<br>` stmt.run(...params);`<br>`}`     | F1-F3; implement list/get/toggle with parameter binding and explicit row-to-public mapping. |
| Database query     | `packages/praxrr-app/src/lib/server/db/db.ts:210-226`                                       | Multi-statement state changes use `db.transaction`; any throw rolls back and is rethrown.                                            | `async transaction<T>(fn: () => T): Promise<T> {`<br>`  this.beginTransaction();`<br>`  try { const result = await fn(); this.commit(); return result; }`<br>`  catch (error) { this.rollback(); ...; throw error; }`<br>`}` | F2, NF2; reconcile discovered/missing rows in one synchronous transaction.                                                            |
| Database query     | `packages/praxrr-app/src/lib/server/db/queries/driftStatus.ts:124-155`                      | A single `INSERT ... ON CONFLICT DO UPDATE` is statement-atomic; avoid nested transactions on the shared connection.                 | `INSERT INTO drift_instance_status (...)`<br>`VALUES (...)`<br>`ON CONFLICT(arr_instance_id) DO UPDATE SET`<br>`  ...`<br>`  updated_at = CURRENT_TIMESTAMP`                                                                 | F2, NF2; upsert manifests while preserving existing `enabled`, and keep the outer reconciliation transaction non-reentrant.           |
| Database query     | `packages/praxrr-app/src/lib/server/db/queries/qualityGoalBindings.ts:29-67`                | Repository objects colocate keyed get/upsert and read back the persisted row after mutation.                                         | `export const qualityGoalBindingQueries = {`<br>` get(...): Row                                                                                                                                                              | undefined { ... },`<br>` upsert(input): Row {`<br>` db.execute(\`INSERT ... ON CONFLICT ...\`);`<br>` return this.get(...);`          | F1-F3; mirror for `pluginRegistryQueries` list/get/reconcile/setEnabled.                    |
| OpenAPI            | `docs/api/v1/openapi.yaml:14-72,691-700`                                                    | Root spec declares a tag and mounts modular path-item refs under `/api/v1`.                                                          | `- name: Canary`<br>`...`<br>`/canary/rollouts:`<br>`  $ref: './paths/canary.yaml#/rollouts'`                                                                                                                                | F3-F4; add Plugins tag and list/get/enable/disable/reload path refs, including `apiVersion` entity params.                            |
| OpenAPI            | `docs/api/v1/openapi.yaml:948-996`                                                          | Every modular schema family is re-exported through `components.schemas`.                                                             | `CanarySettings:`<br>`  $ref: './schemas/canary.yaml#/CanarySettings'`<br>`CanarySettingsUpdate:`<br>`  $ref: './schemas/canary.yaml#/CanarySettingsUpdate'`                                                                 | F4, NF3; add at least one root ref for `schemas/plugins.yaml` so the bundler imports that file, then export all public plugin shapes. |
| OpenAPI            | `scripts/bundle-api.ts:86-137`                                                              | Bundler resolves root path refs and imports every top-level schema from each schema file referenced by root components.              | `const root = readYaml(...);`<br>`for (const [, schemaRef] of Object.entries(rootSchemas)) {`<br>`  ...`<br>`  for (const [name, definition] of Object.entries(fileContent)) schemas[name] = definition;`<br>`}`             | F4; modular plugins YAML must be wired into root before `bundle:api`.                                                                 |
| OpenAPI            | `deno.json:65-72,96-97`                                                                     | Contract pipeline is source YAML -> generated app declarations -> bundled package JSON/types.                                        | `"generate:api-types": "npx openapi-typescript ... -o .../v1.d.ts",`<br>`"bundle:api": "deno run -A scripts/bundle-api.ts",`<br>`"publish:api": "deno task bundle:api ..."`                                                  | F4; run generation then bundle and commit `v1.d.ts`, `praxrr-api/openapi.json`, and `praxrr-api/types.ts`.                            |
| Route integration  | `packages/praxrr-app/src/routes/api/v1/canary/settings/+server.ts:65-131`                   | SvelteKit routes validate payloads at the boundary, call a service/query, map errors, and return JSON.                               | `export const GET: RequestHandler = async () => { ... }`<br>`export const PATCH: RequestHandler = async ({ request }) => {`<br>`  ...`<br>`  return json(canarySettingsQueries.update(parsed.patch));`<br>`}`                | F3, F5; implement list/get/toggle/reload handlers as thin adapters over the plugin service.                                           |
| Route integration  | `packages/praxrr-app/src/lib/server/utils/auth/middleware.ts:24-42`                         | Only explicit `PUBLIC_PATHS` bypass central auth; new `/api/v1/plugins*` routes are protected automatically.                         | `const PUBLIC_PATHS = [`<br>`  '/auth/login',`<br>`  ...`<br>`  '/api/v1/health',`<br>`];`                                                                                                                                   | F9; do not add plugin paths to the public list or invent route-local auth.                                                            |
| Route integration  | `packages/praxrr-app/src/lib/server/security/responses.ts:231-246`                          | A dedicated whitelist mapper produces the public wire object from internal state.                                                    | `export function toSummaryResponse(report): Response {`<br>`  return {`<br>`    engineVersion: report.engineVersion,`<br>`    ...`<br>`  };`                                                                                 | F3, F8, NF4; create one redacted plugin mapper that omits `sourceDir` and raw manifest content.                                       |
| MCP registration   | `packages/praxrr-app/src/lib/server/mcp/tools.ts:134-154,376-381`                           | Read-only tools are entries in `TOOLS`; module-load enforcement rejects any missing `readOnlyHint: true`.                            | `export const TOOLS = [`<br>`  { name: 'list_instances', ...`<br>`    annotations: { readOnlyHint: true },`<br>`    handler: ...`<br>`  }];`                                                                                 | F8; register `list_plugins` with empty/closed input schema and no mutation handler.                                                   |
| MCP registration   | `packages/praxrr-app/src/lib/server/mcp/tools.ts:396-426`                                   | `TOOLS` is the single registry for discovery and calls; handlers pass through standardized redacted serialization/error mapping.     | `listTools() { return TOOLS.map(...); }`<br>`const tool = getTool(name);`<br>`const validated = validateArgs(...);`<br>`return toToolResult(await tool.handler(validated, ctx));`                                            | F8; adding one entry automatically exposes it through `tools/list` and `tools/call`.                                                  |
| MCP registration   | `packages/praxrr-app/src/lib/server/mcp/tools.ts:19-22,178-184`                             | MCP and HTTP share the same response mapper instead of duplicating projection logic.                                                 | `import { computeShield } from '.../service.ts';`<br>`import { toSummaryResponse as toSecuritySummary } from '.../responses.ts';`<br>`...`<br>`handler: async () => toSecuritySummary(await computeShield())`                | F8, NF3-NF4; call the same plugin list service/mapper used by HTTP.                                                                   |
| MCP registration   | `packages/praxrr-app/src/lib/server/mcp/dispatch.ts:35-49`                                  | Transport dispatch routes `tools/list` and `tools/call` to the registry; no endpoint-specific registration exists.                   | `case 'tools/list': return makeResult(id, { tools: listTools() });`<br>`case 'tools/call': {`<br>`  ...`<br>`  return makeResult(id, await callTool(...));`<br>`}`                                                           | F8; no changes needed outside `tools.ts` unless tests/docs require them.                                                              |
| MCP registration   | `docs/api/v1/paths/mcp.yaml:5-14`                                                           | MCP tool schemas are runtime-discovered, while the endpoint description enumerates its read-only surface.                            | `The tool/resource/prompt surface is discovered at`<br>`runtime via tools/list...`<br>`Read-only: exposes Praxrr's configuration...`                                                                                         | F8; update descriptive surface for plugin listing, not a per-tool OpenAPI operation.                                                  |
