| Category                 | File:Lines                                                                                    | Pattern                                                                                                                          | 5-lines-max snippet                                                                                                                                                                                                                                                                                            | Applies To                                                              |
| ------------------------ | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| Error Handling           | `packages/praxrr-app/src/lib/server/plugins/errors.ts:27-34`                                  | Use named, typed domain errors; attach structured detail only where callers need it.                                             | `export class PluginManifestError extends Error {`<br>`constructor(message: string, public readonly issues = []) {`<br>`super(message);`<br>`this.name = 'PluginManifestError';`<br>`}`                                                                                                                        | Repository validation errors; host/reload error classification          |
| Error Handling           | `packages/praxrr-app/src/lib/server/plugins/scan.ts:59-71`                                    | Degrade only expected absence/parse failures; rethrow unexpected filesystem failures.                                            | `catch (error) {`<br>`if (error instanceof Deno.errors.NotFound) return null;`<br>`throw error;`<br>`}`<br>`return { dir: subdir, parseError: ... };`                                                                                                                                                          | Reload scanning and reconciliation preconditions                        |
| Error Handling           | `packages/praxrr-app/src/lib/server/plugins/host.ts:187-214`                                  | Isolate one invalid manifest, log a precise reason, return a discriminated outcome, and continue.                                | `if (!result.ok) {`<br>`await logger.warn('Skipping plugin with invalid manifest', {`<br>`source: LOG_SOURCE, meta: { dir: entry.dir, issues: result.errors }`<br>`});`<br>`return 'rejected';`                                                                                                                | Candidate-snapshot construction and reload summaries                    |
| Error Handling           | `packages/praxrr-app/src/hooks.server.ts:61-69`                                               | Optional subsystem startup is warn-and-continue; persistence/discovery failure must not abort boot.                              | `try {`<br>`await pluginHost.initialize();`<br>`} catch (error) {`<br>`await logger.warn('Failed to initialize plugin host; continuing startup', { ... });`<br>`}`                                                                                                                                             | Startup initialization and NF1 resilience                               |
| Error Handling           | `packages/praxrr-app/src/lib/server/db/db.ts:213-225`                                         | Put reconciliation writes in one transaction; rollback, log, and rethrow on failure.                                             | `this.beginTransaction();`<br>`const result = await fn();`<br>`this.commit();`<br>`... this.rollback();`<br>`throw error;`                                                                                                                                                                                     | Durable scan reconciliation and enable/disable mutations                |
| Logging Patterns         | `packages/praxrr-app/src/lib/server/plugins/host.ts:122-125`                                  | Emit one structured completion summary with a stable source and bounded counters.                                                | `await logger.info('Plugin host initialized', {`<br>`source: LOG_SOURCE,`<br>`meta: { enabled: true, discovered, registered, rejected }`<br>`});`                                                                                                                                                              | Initialize/reload summary response and operational logs                 |
| Logging Patterns         | `packages/praxrr-app/src/lib/server/plugins/host.ts:235-245`                                  | Expected degraded conditions log at debug; unexpected per-plugin failures log at warn with identity/context.                     | `if (error instanceof PluginRuntimeUnavailableError) {`<br>`await logger.debug(... { meta: { pluginId, point } });`<br>`return;`<br>`}`<br>`await logger.warn(... { meta: { pluginId, point, error: String(error) } });`                                                                                       | Dispatch filtering and failure observability                            |
| Logging Patterns         | `packages/praxrr-app/src/lib/server/plugins/scan.ts:111-115`                                  | Log bounded-resource truncation with the affected directory and enforced limit.                                                  | `await logger.warn('Plugin scan truncated: too many candidate directories', {`<br>`source: 'Plugins',`<br>`meta: { dir, limit: MAX_PLUGIN_DIRS }`<br>`});`                                                                                                                                                     | Reload scan guardrails and administrator diagnostics                    |
| Test Patterns            | `packages/praxrr-app/src/tests/db/syncHistoryQueries.test.ts:15-35`                           | Run persistence tests against a fresh real SQLite database and the complete migration chain; restore global config in `finally`. | `await db.initialize();`<br>`await runMigrations();`<br>`await fn();`<br>`... db.close();`<br>`await Deno.remove(tempBasePath, { recursive: true }).catch(() => {});`                                                                                                                                          | Migration, repository, reconciliation, restart-persistence tests        |
| Test Patterns            | `packages/praxrr-app/src/tests/plugins/registry.test.ts:52-78`                                | Assert API-version isolation and case-insensitive identity collisions explicitly.                                                | `const v1 = registry.register(... apiVersion: '1' ...);`<br>`const v2 = registry.register(... apiVersion: '2' ...);`<br>`assertEquals(registry.get('1', id), v1);`<br>`assertThrows(() => registry.register(... case variant ...));`                                                                           | Composite primary key, repository lookup, HTTP entity paths             |
| Test Patterns            | `packages/praxrr-app/src/tests/plugins/scan.test.ts:88-111`                                   | Inject narrow dependencies to deterministically exercise unexpected I/O failures without real permission manipulation.           | `const deps: ScanDeps = {`<br>`readDir: () => { throw new Deno.errors.PermissionDenied('scan blocked'); },`<br>`readTextFile: () => Promise.reject(...)`<br>`};`<br>`await assertRejects(() => scanPluginDir('/any/dir', deps), ...);`                                                                         | Failed reload rollback and previous-snapshot preservation tests         |
| Test Patterns            | `packages/praxrr-app/src/tests/health/recompute.test.ts:118-142`                              | Use a manually released promise gate to prove concurrency behavior and exactly-once persistence.                                 | `const gate = new Promise<void>((resolve) => { release = resolve; });`<br>`const first = recomputeAndPersistInstance(... gatedScorer);`<br>`const second = await recomputeAndPersistInstance(...);`<br>`assertEquals(second.kind, 'in_flight');`<br>`assertEquals(getTrend(id).length, 1);`                    | Concurrent reload serialization/single-flight tests                     |
| Test Patterns            | `packages/praxrr-app/src/tests/base/bundleApiContract.test.ts:181-213`                        | Parse source and bundled OpenAPI, then compare required fields/enums against generated-type samples.                             | `const sourceSchemas = parse(await Deno.readTextFile(...));`<br>`const bundled = JSON.parse(await Deno.readTextFile(...));`<br>`assertEquals(sourceInvalidation.enum, expectedCodes);`<br>`assertEquals(bundledInvalidation.enum, expectedCodes);`<br>`assertEquals(Object.keys(GENERATED_SAMPLE), REQUIRED);` | Plugin OpenAPI, app declarations, and `praxrr-api` lockstep             |
| Test Patterns            | `packages/praxrr-app/src/tests/mcp/mcp.test.ts:654-674`                                       | Plant a real-looking secret in an internal row and assert both mapper keys and serialized bytes exclude it.                      | `const wire = toMcpInstance(instance);`<br>`assert(!Object.keys(wire).includes('api_key'));`<br>`assertEquals(wire.api_key_fingerprint, 'fp-xyz');`<br>`assert(!JSON.stringify(toToolResult(wire)).includes('RAW-API-KEY-VALUE'));`                                                                            | Shared HTTP/MCP plugin mapper; `sourceDir` and raw-manifest redaction   |
| State Changes            | `packages/praxrr-app/src/lib/server/security/dnsTransport.ts:236-246`                         | Store shared in-flight work synchronously before awaiting; clear it only if the same promise still owns the slot.                | `let work = this.inFlight.get(key);`<br>`if (work === undefined) {`<br>`work = this.resolveAndCache(...);`<br>`this.inFlight.set(key, work);`<br>`void work.finally(() => { if (this.inFlight.get(key) === work) this.inFlight.delete(key); });`                                                               | Serialized reloads without duplicate scans/reconciliations              |
| State Changes            | `packages/praxrr-app/src/lib/server/plugins/registry.ts:42-55`                                | Normalize only lookup identity; retain the validated manifest verbatim in the stored entry.                                      | `const key = manifest.id.toLowerCase();`<br>`if (namespace.has(key)) throw new Error(...);`<br>`const entry = { manifest, sourceDir, state: 'registered', ... };`<br>`namespace.set(key, entry);`<br>`return entry;`                                                                                           | Durable `(apiVersion, id COLLATE NOCASE)` identity and no-trim fidelity |
| State Changes            | `packages/praxrr-app/src/lib/server/health/recompute.ts:60-85`                                | Represent concurrent/failed mutations as discriminated outcomes and always release the in-flight guard in `finally`.             | `if (inFlight.has(instance.id)) return { kind: 'in_flight' };`<br>`inFlight.add(instance.id);`<br>`try { ... return { kind: 'ok', report }; }`<br>`catch (...) { return { kind: 'error' }; }`<br>`finally { inFlight.delete(instance.id); }`                                                                   | Reload service result union and HTTP 409/500 mapping                    |
| Reusable Quality Pattern | `packages/praxrr-app/src/lib/server/mcp/serialize.ts:15-26`                                   | Apply redaction as the final serialization transform and provide both text and structured MCP content.                           | `const safe = redactSecrets(value);`<br>`const structuredContent = isPlainRecord(safe) ? safe : { value: safe };`<br>`return {`<br>`content: [{ type: 'text', text: JSON.stringify(safe) }],`<br>`isError: false, structuredContent };`                                                                        | Read-only MCP `list_plugins` adapter                                    |
| Reusable Quality Pattern | `packages/praxrr-app/src/lib/server/plugins/hostContext.ts:88-106`                            | Build public/plugin-boundary payloads by allow-list projection, not deny-list deletion.                                          | `const allowList = CAPABILITY_FIELD_ALLOWLIST[capability];`<br>`if (allowList === undefined                                                                                                                                                                                                                    |                                                                         | !isPlainRecord(source)) return null;`<br>`for (const field of allowList) {`<br>`if (Object.hasOwn(source, field)) projected[field] = ...;`<br>`}` | Central redacted plugin response mapper shared by HTTP and MCP |
| Reusable Quality Pattern | `packages/praxrr-app/src/routes/api/v1/config-health/[instanceId]/recompute/+server.ts:59-80` | Map a service discriminated union to explicit HTTP statuses and a contract-typed success response.                               | `if (outcome.kind === 'in_flight') return json(..., { status: 409 });`<br>`if (outcome.kind === 'skipped') return json(..., { status: 404 });`<br>`if (outcome.kind === 'error') return json(..., { status: 500 });`<br>`return json(toDetailResponse(outcome.report) satisfies DetailResponse);`              | Plugin reload/enable/disable routes and portable contract fidelity      |
